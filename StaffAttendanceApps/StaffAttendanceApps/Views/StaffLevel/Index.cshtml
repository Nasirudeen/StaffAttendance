

@model IEnumerable<StaffAttendanceApps.Models.StaffLevel>
@{
}
@{
    ViewData["Title"] = "StaffLevel Page";
    Layout = "~/Views/Shared/LayoutNew.cshtml";
}
<br />
<br />
<br />
<br />
<br />
<br />
<div class="col-6">
    <h2 class="text-info" style="font-size:35px">Department List</h2>
</div>
<div class="text-center">
    <a href="~/StaffLevel/Create/" class="btn btn-primary bg-c-blue" style="font-size:35px"><i class="fas fa-plus"></i>Create New StaffLevel</a>
</div>
<div class="form-border" style="margin-top:40px;">
    @if (Model.Count() > 0)
    {
        <table class="table table-striped table-hover" id="jayb1">
            <thead style="background-color: azure; font-size: 35px;">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.StaffLevelId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.StaffLevelName)
                    </th>

                    <th>
                        @Html.DisplayNameFor(model => model.Created)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Updated)
                    </th>


                </tr>
            </thead>
            <tbody Style="font-size: 35px;">
                @foreach (var item in Model)
                {
                    var Id = item.StaffLevelId;
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.StaffLevelId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.StaffLevelName)
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.Created)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Updated)
                        </td>


                        <td class="text-center">
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="StaffLevel" asp-route-id="@item.StaffLevelId" asp-action="Edit" class="btn btn-primary mx-2"><i class="fas fa-edit"></i>&nbspEdit</a>
                                <a asp-controller="StaffLevel" asp-route-id="@item.StaffLevelId" asp-action="Delete" class="btn btn-danger mx-2"><i class="far fa-trash-alt"></i>&nbspDelete</a>
                                @*<a asp-controller="Department" asp-route-id="@item.DepartmentId" asp-action="Details" class="btn btn-info mx-2"><i class="fa fa-ban fa-fw"></i>&nbspDetails</a>*@
                            </div>

                        </td>



                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <center> <p><b>No items is created</b></p></center>
    }
</div>
<script>
    $(document).ready(function () {
        $("#jayb1").DataTable({
            dom: 'Bfrtip',
            buttons: [
                'excel', 'pdf', 'print'
            ]
        });

        $("#dialog-form").dialog({
            modal: true
        });

    });

</script>
<script>
        @if (TempData["status"] != null)
        {
            <text>alert('Dept Created Successfully'); </text>
        }